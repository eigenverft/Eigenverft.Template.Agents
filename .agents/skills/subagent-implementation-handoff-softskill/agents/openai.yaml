interface:
  display_name: "Subagent Implementation Handoff"
  short_description: "Orchestrate subagents into local implementation handoffs"
  default_prompt: "Use $subagent-implementation-handoff-softskill as the main-agent orchestrator: brief subagents with minimal unbiased context, assign each run a unique filename-safe UUID token, require complete implementation-near findings in ordered local handoff files, and accept only the created file paths as the subagent response."
