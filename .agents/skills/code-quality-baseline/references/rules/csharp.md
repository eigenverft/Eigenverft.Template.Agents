Name: Program Main Exists
Rule: Every executable C# project must have a valid Main entry point; if it is missing, create the smallest valid entry point immediately; top-level statements count as the compiler-generated Main.

Name: Program Main Is Async When Possible
Rule: Main must always be asynchronous when possible.

Name: Program Main Has Args Array
Rule: Main must always have a string[] args array.
