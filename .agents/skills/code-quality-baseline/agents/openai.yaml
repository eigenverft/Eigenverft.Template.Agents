interface:
  display_name: "Code Quality Baseline"
  short_description: "Load code rules by repository file extension"
  default_prompt: "Use $code-quality-baseline to detect repository file types by extension and read every applicable rule file before implementing or reviewing code."
