# Agent instructions

If this file is read from the canonical repository that maintains and distributes this instruction set, stop processing after this paragraph: do not read or execute the referenced `AGENTS/` guidance or run-once instructions, and continue directly with the user's request and normal conversation. This exception applies only to the template source repository; distributed copies in consuming repositories follow the remaining instructions normally.

At the start of each new chat session, read `AGENTS/GUIDE.md` and any project guidance in `AGENTS/RUNBOOK/` relevant to the task. Then follow `AGENTS/RUNONCE.md` and execute every instruction in `AGENTS/RUNONCE/` once before doing other work.
